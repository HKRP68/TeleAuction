version: '3.8'

services:
  bot:
    build: .
    env_file: .env
    volumes:
      # Persist SQLite database across restarts
      - ./data:/app/data
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - DATABASE_PATH=/app/data/auction.db
